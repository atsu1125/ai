version: "3"

services:
  ai:
    build: .
    image: ghcr.io/atsu1125/ai:latest
    restart: always
    environment:
      FC_LANG: ja
      TZ: Asia/Tokyo
    volumes:
      - ./config.json:/ai/config.json:ro
      - ./memory:/ai/memory
